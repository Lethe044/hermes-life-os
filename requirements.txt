openai>=1.0.0
rich>=13.0.0
pyyaml>=6.0
pytest>=7.0.0

# Optional: only needed if you use --provider anthropic
anthropic>=0.34.0

# Optional: only needed for demo/dashboard.py
matplotlib>=3.7.0

# Optional: only needed if you set LIFE_OS_ENCRYPTION_KEY
cryptography>=42.0.0
